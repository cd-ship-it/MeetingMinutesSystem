# Copy to .env in project root and fill in values.
# Do not commit .env.

APP_ENV=development
APP_URL=http://localhost:8888/MeetingMinutesSystem/public

# Database (local example)
DB_HOST=localhost
DB_PORT=8889
DB_NAME=crossp11_cbi
DB_USER=root
DB_PASSWORD=root

# Google OAuth (production admin) - optional in development
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# Or set GOOGLE_CREDENTIALS_PATH to client_secret_*.json path
